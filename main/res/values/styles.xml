<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="noAnimTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="questionTextViews" parent="AppTheme">
        <item name="android:padding">10dp</item>
        <item name="android:textSize">18dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="answerTextViews" parent="AppTheme">
        <item name="android:textSize">15dp</item>
        <item name="android:gravity">center|right</item>
        <item name="android:textColor">@color/textColorParagraph</item>
    </style>

    <style name="questionImageViews" parent="AppTheme">
        <item name="android:padding">5dp</item>
        <item name="android:gravity">center</item>
        <item name="android:tint">@color/colorPrimaryLight</item>
    </style>

</resources>
